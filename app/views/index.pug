extends layout

block title
  title ♨

append styles
  link(href='/css/navbar-fixed-top.css', rel='stylesheet')

block body
  +navbar('index')
  .container
    .jumbotron
      h1 ♨
      p うちまガチャ以外は準備中
      p
        a.btn.btn-lg.btn-primary(href='/auth', role='button') Auth
